server:
  port: 8080

spring:
  datasource:
    url: "jdbc:postgresql://localhost:5432/ToDoListDB"
    username: "postgres"
    password: "12345678"
    driver-class-name: "org.postgresql.Driver"
  jpa:
    database: POSTGRESQL  
    hibernate:
      ddl-auto: update
    show-sql: true

